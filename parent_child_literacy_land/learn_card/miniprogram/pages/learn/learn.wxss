/* 1. 容器：不再固定 100vh，而是让内容（图片）撑开高度 */
.container {
  position: relative; /* 关键！作为点击区域的定位参照物 */
  width: 100%;        /* 宽度占满屏幕 */
  /* height: 100vh;   <-- 删掉这一行，不要限制高度 */
  overflow: hidden;
  padding: 0;         /* 确保没边距 */
  margin: 0;
}

/* 2. 图片：取消绝对定位，回归文档流，负责撑开容器 */
.background-image {
  display: block;     /* 消除图片底部的微小缝隙 */
  width: 100%;        /* 宽度跟随容器 */
  height: auto;       /* 高度自动 */
  /* position: absolute; <-- 删掉这行 */
}

/* 3. 点击区域：现在它是相对于“图片的高度”来定位的了 */
.clickable-area {
  position: absolute;
  z-index: 100;
  /* --- 调试模式开关 --- */
  /* 如果还要微调位置，保留下面两行；如果想发布，删掉或注释掉 */
  border: 4rpx solid rgba(255, 215, 0, 0.6); /* 金色半透明边框 */
  border-radius: 12rpx;
  background-color: rgba(0, 0, 0, 0.9);
  box-shadow: 0 0 10rpx rgba(0,0,0,0.6);
  /* 让标签内容居中 */
  display: flex;
  justify-content: center;
  align-items: center;
}

/* 4. 如果你想保留文字标签，不想保留就整段删掉 */
.label-tag {
  /* background-color: rgba(0, 0, 0, 0.6);  */
  color: #fff;
  font-size: 28rpx;
  font-weight: bold;
  padding: 4rpx 12rpx;
  border-radius: 8rpx;
  display: flex;
  flex-direction: column; 
  align-items: center;
  justify-content: center;
  white-space: nowrap; /* 防止文字换行 */
}

/* 新增：点击时的动效 */
.clicked-effect {
  /* 稍微缩小一点，产生按压感 */
  transform: scale(0.9); 
  /* 加个白色高亮背景，像闪光灯一样亮一下 */
  background-color: rgba(255, 255, 255, 0.5) !important; 
  border-radius: 8px;
  transition: all 0.1s;
}